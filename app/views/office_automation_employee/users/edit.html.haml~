.row
  .col-md-2
  .col-md-8
    = render "office_automation_employee/shared/flash"
    .panel.panel-default
      .panel.panel-heading
        %strong Edit Profile

      .panel.panel.body
        .container-fluid
          .col-md-2
          .col-md-8
            %ul.nav.nav-tabs
              %li.active.public-tab
                %a{data: {toggle: 'tab'}, href: "#public"}
                  Public Profile
              %li.private-tab
                %a{data: {toggle: "tab"}, href: "#private"}
                  Personal Profile
              %li.password-tab
                %a{data: {toggle: "tab"}, href: "#password"}
                  Change Password
              %li.document-tab
                %a{data: {toggle: "tab"}, href: "#document"}
                  Documents

            %br
            .tab-content
              .tab-pane.active#public
                = simple_form_for(@user, url: office_automation_employee.company_user_path(@user.company, @user), method: :put, input_html: { role: :form } ) do |user|
                  .form-group
                    = user.simple_fields_for :profile, @profile do |profile|
                      .row
                        = render partial: "office_automation_employee/users/profile", locals: {profile: profile}
                        .form-group
                        .col-md-6
                          = image_tag(user.object.image_url, size: "150x150")
                          = user.input :image, as: :file

                      = user.hidden_field :update_type, value: 'profile'
                    
                    .form-group
                      = user.submit "Update Profile", class: "btn btn-primary"
              
              .tab-pane#private
                = simple_form_for(@user, url: office_automation_employee.user_registration_path, method: :patch, input_html: { role: :form } ) do |user|
                  .form-group
                    = user.simple_fields_for :personal_profile, @personal_profile do |personal_profile|
                      = render partial: "office_automation_employee/users/personal_profile", locals: {personal_profile: personal_profile}
                  .form-group
                    = user.hidden_field :update_type, value: 'personal_profile'
                    = user.submit "Update Profile", class: "btn btn-primary"
                
